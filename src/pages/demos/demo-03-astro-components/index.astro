---
import Greeting from './Greeting.astro';
---
<html>
  <head>
    <title>Astro Components Demo</title>
    <style>
      body {
        font-family: 'Segoe UI', Arial, sans-serif;
        background: #f8fafc;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 500px;
        margin: 3rem auto;
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 4px 24px #0001;
        padding: 2.5rem 2rem 2rem 2rem;
        text-align: center;
      }
      h1 {
        color: #3b82f6;
        font-size: 2rem;
        margin-bottom: 1.5rem;
      }
      .desc {
        color: #64748b;
        font-size: 1.1rem;
        margin-bottom: 2rem;
      }
      .greetings {
        display: flex;
        flex-direction: column;
        gap: 1.2rem;
        align-items: center;
        margin-top: 1.5rem;
      }
      .greeting-card {
        background: #f1f5f9;
        border-radius: 8px;
        padding: 1rem 2rem;
        font-size: 1.1rem;
        color: #222;
        box-shadow: 0 1px 4px #0001;
        min-width: 220px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Astro Components Demo</h1>
      <div class="desc">
        This demo shows how to create and reuse Astro components with props.<br />
        Each greeting below is a separate instance of the same component.
      </div>
      <div class="greetings">
        <div class="greeting-card"><Greeting name="Astro" /></div>
        <div class="greeting-card"><Greeting name="World" /></div>
        <div class="greeting-card"><Greeting name="Audience" /></div>
      </div>
    </div>
  </body>
</html>
