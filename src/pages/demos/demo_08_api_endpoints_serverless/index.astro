---
let data = { message: 'No API response' };
try {
  const { origin } = new URL(Astro.request.url);
  const res = await fetch(`${origin}/demos/demo_08_api_endpoints_serverless/api/hello.json`);
  data = await res.json();
} catch (e) {
  data = { message: 'API fetch failed' };
}
---
<html>
  <head>
    <title>API Endpoints Demo</title>
    <link rel="stylesheet" href="/src/styles/global.css">
  </head>
  <body class="font-sans bg-slate-50 m-0 p-0">
    <div class="max-w-lg mx-auto mt-12 bg-white rounded-2xl shadow-lg shadow-black/5 px-8 py-10 text-center">
      <h1 class="text-blue-500 text-3xl mb-6 font-normal">API Endpoints Demo</h1>
      <div class="text-slate-500 text-lg mb-8">
        This demo shows how to fetch data from an Astro API endpoint at runtime and display the result below.
      </div>
      <span class="text-blue-500 font-medium mb-2 block">API Response:</span>
      <div class="bg-slate-100 rounded-lg px-8 py-6 text-lg text-gray-800 shadow-sm shadow-black/5 min-w-56 inline-block">
        <p>API says: <b class="font-semibold">{data.message}</b></p>
      </div>
    </div>
  </body>
</html>
