---
import Counter from './_Counter.jsx';
---
<html>
  <head>
    <title>Client Directives Demo</title>
    <link rel="stylesheet" href="/src/styles/global.css">
  </head>
  <body class="font-sans bg-slate-50 m-0 p-0">
    <div class="max-w-2xl mx-auto mt-12 bg-white rounded-2xl shadow-lg shadow-black/5 px-8 py-10 text-center">
      <h1 class="text-blue-500 text-3xl mb-6 font-normal">Client Directives & Hydration</h1>
      <div class="text-slate-500 text-lg mb-8">
        This demo shows Astro's partial hydration with <code class="bg-slate-100 px-1 py-0.5 rounded text-sm">client:load</code> and <code class="bg-slate-100 px-1 py-0.5 rounded text-sm">client:visible</code> directives.<br />
        Only the interactive components load JavaScript!
      </div>
      <div class="mb-9">
        <span class="text-blue-500 font-medium mb-2 block">Static Astro component (no JS):</span>
        <div class="bg-slate-100 rounded-lg px-8 py-5 text-lg text-gray-800 shadow-sm shadow-black/5 min-w-56 mx-auto mb-5 inline-block">
          <span>Pure Astro, no interactivity</span>
        </div>
      </div>
      <div class="mb-9">
        <span class="text-blue-500 font-medium mb-2 block">Hydrated React Counter (<code class="bg-slate-100 px-1 py-0.5 rounded text-sm">client:load</code>):</span>
        <div class="bg-slate-100 rounded-lg px-8 py-5 text-lg text-gray-800 shadow-sm shadow-black/5 min-w-56 mx-auto mb-5 inline-block">
          <Counter client:load />
        </div>
      </div>
      <div class="h-80"></div>
      <div class="mb-9">
        <span class="text-blue-500 font-medium mb-2 block">Hydrated React Counter (<code class="bg-slate-100 px-1 py-0.5 rounded text-sm">client:visible</code>):</span>
        <div class="bg-slate-100 rounded-lg px-8 py-5 text-lg text-gray-800 shadow-sm shadow-black/5 min-w-56 mx-auto mb-5 inline-block">
          <Counter client:visible />
        </div>
      </div>
    </div>
  </body>
</html>
