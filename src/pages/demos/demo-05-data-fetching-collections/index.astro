---
let posts = [];
try {
  // Use Astro.request.url to build the absolute URL for SSR compatibility
  const { origin } = new URL(Astro.request.url);
  const res = await fetch(`${origin}/api/posts.json`);
  posts = await res.json();
} catch (e) {
  posts = [];
}
---
<html>
  <head>
    <title>Data Fetching Demo</title>
    <style>
      body {
        font-family: 'Segoe UI', Arial, sans-serif;
        background: #f8fafc;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 500px;
        margin: 3rem auto;
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 4px 24px #0001;
        padding: 2.5rem 2rem 2rem 2rem;
        text-align: center;
      }
      h1 {
        color: #3b82f6;
        font-size: 2rem;
        margin-bottom: 1.5rem;
      }
      .desc {
        color: #64748b;
        font-size: 1.1rem;
        margin-bottom: 2rem;
      }
      ul {
        list-style: none;
        padding: 0;
        margin: 0 auto;
        max-width: 300px;
      }
      li {
        background: #f1f5f9;
        border-radius: 8px;
        margin-bottom: 1rem;
        padding: 1rem;
        font-size: 1.1rem;
        box-shadow: 0 1px 4px #0001;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Data Fetching Demo</h1>
      <div class="desc">
        This demo fetches data from an API endpoint at runtime and displays it below.
      </div>
      <ul>
        {posts.length ? posts.map((post: { title: string }) => <li>{post.title}</li>) : <li>No posts found.</li>}
      </ul>
    </div>
  </body>
</html>
