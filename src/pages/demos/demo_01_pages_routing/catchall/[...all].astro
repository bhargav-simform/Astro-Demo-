---
export async function getStaticPaths() {
  return [
    { params: { all: 'example' } },
    { params: { all: 'demo/path' } },
    { params: { all: 'test/multiple/segments' } },
  ];
}

const { all } = Astro.params;
const allSegments = Array.isArray(all) ? all : all ? [all] : [];
---
<html>
  <head>
    <title>Catch-all Route</title>
    <link rel="stylesheet" href="/src/styles/global.css">
  </head>
  <body class="font-sans bg-slate-50 m-0 p-0">
    <div class="max-w-lg mx-auto mt-12 bg-white rounded-2xl shadow-lg shadow-black/5 px-8 py-10 text-center">
      <h2 class="text-blue-500 text-3xl mb-4 font-normal">Catch-all Route</h2>
      <div class="text-slate-500 text-lg mb-8">
        This route matches any path after <code class="bg-slate-100 px-1 py-0.5 rounded text-sm">/catchall/</code>.<br />
        The <code class="bg-slate-100 px-1 py-0.5 rounded text-sm">[...all].astro</code> file receives all segments as an array.
      </div>
      <div class="text-lg text-slate-900 mb-6">
        Segments: <b class="font-semibold">{allSegments.length ? allSegments.join(" / ") : "(no segments)"}</b>
      </div>
      <a class="text-blue-500 no-underline font-medium transition-colors duration-200 hover:text-slate-800" href="/demos/demo_01_pages_routing/">← Back to Demo Home</a>
    </div>
  </body>
</html>
