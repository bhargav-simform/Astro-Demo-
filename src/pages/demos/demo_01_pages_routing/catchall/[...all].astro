---
const { all } = Astro.params;
const allSegments = Array.isArray(all) ? all : all ? [all] : [];
---
<html>
  <head>
    <title>Catch-all Route</title>
    <style>
      body {
        font-family: 'Segoe UI', Arial, sans-serif;
        background: #f8fafc;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 500px;
        margin: 3rem auto;
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 4px 24px #0001;
        padding: 2.5rem 2rem 2rem 2rem;
        text-align: center;
      }
      h2 {
        color: #3b82f6;
        font-size: 2rem;
        margin-bottom: 1rem;
      }
      .desc {
        color: #64748b;
        font-size: 1.1rem;
        margin-bottom: 2rem;
      }
      a {
        color: #3b82f6;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.2s;
      }
      a:hover {
        color: #1e293b;
      }
      .segments {
        font-size: 1.1rem;
        color: #0f172a;
        margin-bottom: 1.5rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>Catch-all Route</h2>
      <div class="desc">
        This route matches any path after <code>/catchall/</code>.<br />
        The <code>[...all].astro</code> file receives all segments as an array.
      </div>
      <div class="segments">
        Segments: <b>{allSegments.length ? allSegments.join(" / ") : "(no segments)"}</b>
      </div>
      <a href="/demos/demo_01_pages_routing/">← Back to Demo Home</a>
    </div>
  </body>
</html>
